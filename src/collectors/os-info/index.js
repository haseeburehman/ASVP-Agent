/**
 * Operating-system information collector placeholder.
 *
 * Will collect normalized platform, distribution, version, architecture,
 * hostname, kernel, uptime, and hardware metadata using read-only operations.
 */
