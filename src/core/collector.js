/**
 * Collector contract placeholder.
 *
 * Will define the stable plugin interface, metadata, capability checks,
 * cancellation behavior, normalized result envelope, and error semantics
 * implemented by every collector.
 */
